ollama-info v1.9 atomic requirements

AR-001 The package scripts must identify release v1.9 in executable signatures.
AR-002 README.md must not use release-specific wording or version labels.
AR-003 README.md must explain every supported wrapper command.
AR-004 README.md must explain what each command does.
AR-005 README.md must explain when to use each command.
AR-006 README.md must explain how to interpret benchmark result fields.
AR-007 README.md must document result states and sample statuses.
AR-008 README.md must document multi-model aggregate ZIP behavior.
AR-009 Multi-model test must create one aggregate run directory and one ZIP archive by default.
AR-010 Multi-model bench must create one aggregate run directory and one ZIP archive by default.
AR-011 Multi-model embed-test must create one aggregate run directory and one ZIP archive by default.
AR-012 Multi-model sub-runs must execute with --no-zip to avoid per-model ZIP duplication.
AR-013 Aggregate multi-model output must include a Markdown summary and machine-readable index CSV.
AR-014 Route-only mode must still work for multiple models without executing benchmarks.
AR-015 Role-aware bench routing must still route embedding-only models to embedding mode.
AR-016 ADOS capability profile visible rows must count coding, essay, and internet-access visible-answer speeds.
AR-017 Thinking-only rows must not count as visible-answer speed.
AR-018 Streaming timestamp sidecars must be removed after durable stream-metrics JSON is written.
AR-019 Monitored runs must avoid duplicate nested direct-test terminal-summary files when terminal summary printing is disabled.
AR-020 Orchestrator Markdown must avoid embedding the full compact terminal summary when terminal-summary.txt already owns that surface.
AR-021 Package evidence must use one current logical evidence ledger.
AR-022 Package QA files must use stable names where practical.
AR-023 Package must exclude run archives, nested ZIPs, caches, pyc files, and legacy debris.
AR-024 Verification must include syntax checks.
AR-025 Verification must include aggregate multi-model ZIP behavior under a deterministic fake harness.
AR-026 Verification must include route-only multi-model test and bench checks.
AR-027 Verification must include README no-version scan.
AR-028 Verification must include evidence-ledger JSONL parse.
AR-029 Verification must include extracted archive hygiene validation.
AR-030 Final response must summarize performance interpretation and model recommendations for OpenCode, Cursor, Hermes Agent, and ADOS.
