# Request record

User requested production-grade v1.12 with:

- `ollama test --full <models>` to run all tests.
- `--min-context 65536` as the context/Hermes gate.
- redesigned final summaries in table format.
- proper context-window collection for each tested model.
- README updates with project description, GOAL, objectives, command details, and test config nuances.
