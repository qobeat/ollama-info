# Reflection v1.13

The release remains a local diagnostic toolkit, not a deep model-quality benchmark. v1.13 improves production trust by making evidence labels stricter and by avoiding overclaiming when tests are skipped, ambiguous, or text-only. Vision is now explicit and image-backed rather than implied by model name.
