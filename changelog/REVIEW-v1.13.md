# Review v1.13

v1.13 is based on the v1.12 quality evaluation and uploaded runtime zips. The release fixes production blockers: invalid vision command, bash wrapper drift, stale exit-code parsing, brittle chunk-line capability validation, unsafe decision-grade logic, skipped-context wording, and ambiguous aggregate ranking.

The package intentionally keeps v1.12's useful surfaces: `--full`, `--min-context`, table summaries, model scorecards, context summaries, Hermes gate, settings artifacts, and aggregate recommendations.
